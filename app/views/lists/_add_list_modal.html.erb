<div class="modal fade" id="addListModal" tabindex="-1" aria-labelledby="addListModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-lg">
    <div class="modal-content shadow-lg overflow-hidden rounded-4 border-0">

      <div class="row g-0">
        <!-- Left Side: Image -->
        <div class="col-md-6 d-none d-md-block">
          <img src="<%= asset_path('cinema.png') %>"
               alt="Movie background"
               class="img-fluid h-100 w-100 object-fit-cover">
        </div>

        <!-- Right Side: Form -->
        <div class="col-md-6 bg-white p-5 d-flex flex-column justify-content-center">
          <div class="d-flex justify-content-between align-items-start mb-3">
            <h5 class="modal-title fw-bold text-uppercase" id="addListModalLabel">
              Create a New List
            </h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>

          <!-- @list comes from the controller; binds the form to the List model (fallback creates a new one if @list is nil) -->
          <%= simple_form_for(@list || List.new) do |f| %>
            <div class="mb-3">
              <%= f.input :name,
                          label: false,
                          placeholder: "Enter a name for your list...",
                          input_html: { class: "form-control border-2 border-dark-subtle py-2" } %>
            </div>

            <div class="d-grid">
              <%= f.submit "Create List", class: "btn btn-danger text-white fw-bold py-2 fs-6 rounded-3 shadow-sm" %>
            </div>
          <% end %>
        </div>
      </div>

    </div>
  </div>
</div>
