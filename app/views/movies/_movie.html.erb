<div class="col-12 col-sm-6 col-md-4 col-lg-3 col-xl-2">
  <div class="card h-100 bg-dark text-light shadow-sm border-0 movie-card">
    <% if movie.poster_url.present? %>
      <img src="<%= movie.poster_url %>" class="card-img-top object-fit-cover" style="height: 300px;" alt="<%= movie.title %>">
    <% else %>
      <div class="bg-secondary d-flex align-items-center justify-content-center" style="height: 300px;">
        <span class="text-muted">No image</span>
      </div>
    <% end %>

    <div class="card-body d-flex flex-column justify-content-between">
      <div>
        <h6 class="fw-bold"><%= movie.title %></h6>
        <p class="card-text text-secondary small"><%= truncate(movie.overview, length: 80) %></p>
      </div>

      <% if movie.respond_to?(:rating) && movie.rating.present? %>
        <span class="badge bg-danger mt-2 align-self-start">⭐ <%= movie.rating.round(1) %></span>
      <% end %>
    </div>
  </div>
</div>
